"use client";

import { LiveDashboard } from "@/components/evox/LiveDashboard";

/**
 * /live — Public demo page
 * Shows agents working in real-time
 * Read-only, no mutations
 */
export default function LivePage() {
  return <LiveDashboard />;
}
